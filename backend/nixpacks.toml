[phases.setup]
nixPkgs = ["python310"]

[phases.install]
cmd = "pip install -r requirements.txt"

[phases.build]
cmd = "python deploy_migrations.py"

[start]
cmd = "python -m gunicorn app:app --bind 0.0.0.0:$PORT --workers 2 --timeout 30"

[variables]
FLASK_ENV = "production"
PYTHONPATH = "/app"