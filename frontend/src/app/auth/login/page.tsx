import { LoginForm } from '@/components/auth/LoginForm';

export default function LoginPage() {
  return <LoginForm />;
}

export const metadata = {
  title: 'Login | FisioFlow',
  description: 'Entre na sua conta do FisioFlow',
};