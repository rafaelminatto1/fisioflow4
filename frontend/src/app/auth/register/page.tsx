import { RegisterForm } from '@/components/auth/RegisterForm';

export default function RegisterPage() {
  return <RegisterForm />;
}

export const metadata = {
  title: 'Criar Conta | FisioFlow',
  description: 'Crie sua conta no FisioFlow',
};